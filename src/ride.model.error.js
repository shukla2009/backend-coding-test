'use strict';

const START_LAT_LONG_INVALID = 'Start latitude and longitude must be between -90 - 90 and -180 to 180 degrees respectively';
const END_LAT_LONG_INVALID = 'End latitude and longitude must be between -90 - 90 and -180 to 180 degrees respectively';
const RIDER_NAME_INVALID ='Rider name must be a non empty string';
const DRIVER_NAME_INVALID ='Driver name must be a non empty string';
const DRIVER_VEHICLE_INVALID ='Driver vehicle must be a non empty string';

module.exports = {START_LAT_LONG_INVALID,END_LAT_LONG_INVALID,RIDER_NAME_INVALID,DRIVER_NAME_INVALID,DRIVER_VEHICLE_INVALID}